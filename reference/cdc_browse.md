# Open Dataset in Browser

Opens the CDC data portal page for a dataset in your default web
browser.

## Usage

``` r
cdc_browse(dataset_id)
```

## Arguments

- dataset_id:

  Character. The dataset identifier.

## Examples

``` r
if (FALSE) { # \dontrun{
cdc_browse("swc5-untb")
} # }
```
