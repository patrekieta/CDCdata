% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{cdc_fetch}
\alias{cdc_fetch}
\title{Fetch Large CDC Datasets with Pagination}
\usage{
cdc_fetch(
  dataset_id,
  select = NULL,
  where = NULL,
  order = NULL,
  q = NULL,
  max_rows = Inf,
  page_size = 1000,
  progress = interactive(),
  as_csv = FALSE
)
}
\arguments{
\item{dataset_id}{Character. The unique dataset identifier (e.g., "swc5-untb"
for PLACES data). You can find this in the dataset URL or via \code{cdc_datasets()}.}

\item{select}{Character vector. Columns to return. Use \code{NULL} for all columns.}

\item{where}{Character. SoQL WHERE clause for filtering rows.
Example: \code{"year = '2023' AND stateabbr = 'MN'"}}

\item{order}{Character. Column(s) to sort by. Use \code{"column DESC"} for descending.}

\item{q}{Character. Full-text search query across all text fields.}

\item{max_rows}{Integer. Maximum total rows to fetch. Use \code{Inf} for all rows.}

\item{page_size}{Integer. Rows per API request (default 1000, max 50000).}

\item{progress}{Logical. Show progress bar? Default \code{TRUE} for interactive sessions.}

\item{as_csv}{Logical. If \code{TRUE}, requests data in CSV format instead of JSON.
CSV can be faster for large datasets and may handle some data types differently.
Default is \code{FALSE}.}
}
\value{
A data.frame containing all fetched rows.
}
\description{
Retrieves large datasets by automatically paginating through all results.
Shows a progress bar for long-running queries.
}
\examples{
\dontrun{
places_mn <- cdc_fetch(
  "swc5-untb",
  where = "stateabbr = 'MN'",
  max_rows = 5000
)

all_data <- cdc_fetch("swc5-untb", where = "year = '2023'")

# Use CSV format for faster downloads
cdc_fetch("swc5-untb", where = "stateabbr = 'CA'", as_csv = TRUE)
}

}
\seealso{
\code{\link[=cdc_query]{cdc_query()}}
}
