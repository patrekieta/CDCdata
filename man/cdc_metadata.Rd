% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{cdc_metadata}
\alias{cdc_metadata}
\title{Get Dataset Metadata}
\usage{
cdc_metadata(
  dataset_id,
  include_columns = TRUE,
  include_rowcount = TRUE,
  cleaned = TRUE,
  progress = interactive()
)
}
\arguments{
\item{dataset_id}{Character. The dataset identifier.}

\item{include_columns}{Logical. If \code{TRUE} (default), includes column-level metadata.}

\item{include_rowcount}{Logical. If \code{TRUE} (default), creates row count metadata. Row count is not available
in the api metadata so this has to be created manually using \code{\link[=cdc_count]{cdc_count()}}.}

\item{cleaned}{Logical. If \code{TRUE} (default), returns the cleaned metadata. If \code{FALSE}, provides the raw metadata
including row_count (unless \code{include_rowcount = FALSE}).}

\item{progress}{Logical. If \code{TRUE} (default), shows function progress outputs in console.}
}
\value{
A list containing:
\itemize{
\item \code{id}: Dataset identifier
\item \code{name}: Dataset title
\item \code{description}: Full description
\item \code{attribution}: Data source attribution
\item \code{category}: Dataset category
\item \code{tags}: Associated tags
\item \code{created_at}: Creation timestamp
\item \code{updated_at}: Last update timestamp
\item \code{row_count}: Row count (if \code{include_rowcount = TRUE})
\item \code{columns}: data.frame of column metadata (if \code{include_columns = TRUE})
}
}
\description{
Retrieves detailed metadata for a specific CDC dataset, including
column names, types, descriptions, and dataset-level information.
}
\examples{
\dontrun{
cdc_metadata("swc5-untb")
}

}
