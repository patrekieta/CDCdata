% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{cdc_query}
\alias{cdc_query}
\title{Query a CDC Dataset}
\usage{
cdc_query(
  dataset_id,
  select = NULL,
  where = NULL,
  order = NULL,
  group = NULL,
  having = NULL,
  q = NULL,
  limit = 1000,
  offset = NULL,
  as = c("dataframe", "list", "raw"),
  as_csv = FALSE,
  progress = interactive()
)
}
\arguments{
\item{dataset_id}{Character. The unique dataset identifier (e.g., "swc5-untb"
for PLACES data). You can find this in the dataset URL or via \code{cdc_datasets()}.}

\item{select}{Character vector. Columns to return. Use \code{NULL} for all columns.}

\item{where}{Character. SoQL WHERE clause for filtering rows.
Example: \code{"year = '2023' AND stateabbr = 'MN'"}}

\item{order}{Character. Column(s) to sort by. Use \code{"column DESC"} for descending.}

\item{group}{Character. Column(s) to group by for aggregation.}

\item{having}{Character. SoQL HAVING clause for filtering aggregated results.}

\item{q}{Character. Full-text search query across all text fields.}

\item{limit}{Integer. Maximum rows to return. Default 1000, max 50000 per request.}

\item{offset}{Integer. Number of rows to skip (for manual pagination).}

\item{as}{Character. Output format: "dataframe" (default), "list", or "raw".}

\item{as_csv}{Logical. If \code{TRUE}, requests data in CSV format instead of JSON.
CSV can be faster for large datasets and may handle some data types differently.
Default is \code{FALSE}.}
}
\value{
A data.frame (default), list, or raw string depending on \code{as}.
}
\description{
Fetches data from a CDC dataset on data.cdc.gov using the Socrata API.
Supports flexible querying via SoQL (\href{https://dev.socrata.com/docs/functions/#,}{Socrata Query Language}).
}
\details{
\subsection{SoQL Reference}{

Common WHERE clause operators:
\itemize{
\item Comparison: \code{=}, \code{!=}, \code{<}, \code{>}, \code{<=}, \code{>=}
\item Logical: \code{AND}, \code{OR}, \code{NOT}
\item Text: \verb{LIKE '\%pattern\%'}, \code{starts_with()}, \code{contains()}
\item Null checks: \verb{IS NULL}, \verb{IS NOT NULL}
\item Lists: \code{IN ('a', 'b', 'c')}
}

Aggregation functions (use with \code{group}):
\itemize{
\item \verb{count(*)}, \code{sum()}, \code{avg()}, \code{min()}, \code{max()}
}
}

\subsection{JSON vs CSV}{

JSON (default) preserves data types and handles nested structures.
CSV may be faster for large flat datasets but converts everything to strings.
}
}
\examples{
\dontrun{
cdc_query("swc5-untb", limit = 100)

cdc_query(
  "swc5-untb",
  select = c("stateabbr", "locationname", "access2_crudeprev"),
  where = "stateabbr = 'MN' AND year = '2023'",
  order = "access2_crudeprev DESC",
  limit = 50
)

cdc_query(
  "swc5-untb",
  select = c("stateabbr", "count(*) as n"),
  group = "stateabbr",
  order = "n DESC"
)

# Use CSV format for potentially faster downloads
cdc_query("swc5-untb", limit = 100, as_csv = TRUE)
}

}
\seealso{
\code{\link[=cdc_fetch]{cdc_fetch()}} for paginated retrieval of large datasets.
}
