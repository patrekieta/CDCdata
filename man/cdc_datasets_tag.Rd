% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{cdc_datasets_tag}
\alias{cdc_datasets_tag}
\title{Search Datasets by Tag}
\usage{
cdc_datasets_tag(tag, query = NULL, limit = 20, exact = FALSE)
}
\arguments{
\item{tag}{Character. The tag name to search for.
Supports partial matching (case-insensitive) by default.}

\item{query}{Character. Optional additional search terms to filter results.}

\item{limit}{Integer. Maximum number of results (default 20, max 100).}

\item{exact}{Logical. If \code{TRUE}, requires exact tag match. If \code{FALSE}
(default), uses partial matching.}
}
\value{
A data.frame with columns:
\itemize{
\item \code{id}: Dataset identifier for use with \code{cdc_query()}
\item \code{name}: Dataset title
\item \code{description}: Brief description
\item \code{category}: Dataset category
\item \code{tags}: Comma-separated list of tags
\item \code{updated_at}: Last update timestamp
\item \code{rows}: Approximate row count (if available)
\item \code{url}: Direct link to dataset page
}
}
\description{
Finds datasets with a specific tag on data.cdc.gov.
Use \code{cdc_tags()} to see available tags.
}
\examples{
\dontrun{
# Find datasets tagged with "covid-19"
cdc_datasets_tag("covid-19")

# Find mortality-related datasets
cdc_datasets_tag("mortality")

# Search within tagged datasets
cdc_datasets_tag("vaccination", query = "children")

# Exact tag match
cdc_datasets_tag("brfss", exact = TRUE)
}

}
\seealso{
\code{\link[=cdc_tags]{cdc_tags()}} to list available tags,
\code{\link[=cdc_datasets]{cdc_datasets()}} for general keyword search.
}
